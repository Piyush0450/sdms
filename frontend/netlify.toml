[build]
  command = "npm run build"
  publish = "dist"

# Proxy /api requests to the Render Backend
[[redirects]]
  from = "/api/*"
  to = "https://sdms-backend-1f01.onrender.com/api/:splat"
  status = 200
  force = true

# SPA Fallback: Redirect everything else to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
