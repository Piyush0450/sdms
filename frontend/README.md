# SDMS Frontend (Vite + React + Tailwind)

## Quick Start
```bash
cd frontend
npm install
npm run dev
```
By default, API calls to `/api/*` are proxied to `http://localhost:5000` (configure in `vite.config.js`).

## Build
```bash
npm run build
npm run preview
```
