services:
  - type: web
    name: sdms-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python -m scripts.reset_db_full && gunicorn "app:create_app()"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
